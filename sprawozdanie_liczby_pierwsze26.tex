\documentclass[10pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{babel}
\babelprovide[import, main]{polish}
\usepackage{amsmath}
\usepackage[a4paper, margin=1.5cm]{geometry}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{graphicx}

\lstdefinestyle{mystyle}{
    basicstyle=\ttfamily\small,
    frame=single,
    breaklines=true,
    captionpos=b,
    showstringspaces=false
}

\title{Projekt I przetwarzanie równoległe 2025/26 SS, OpenMP, Vtune}
\author{Emilia Bonk 160108 \\ Szymon Urban 160271}
\date{}

\begin{document}

\maketitle

\section*{Wstęp.}

\begin{itemize}
\item \textbf{Nazwa przedmiotu:} Laboratorium z przetwarzania równoległego
\item \textbf{Autorzy:} Emilia Bonk (160108), Szymon Urban (160271)
\item \textbf{Grupa dziekańska:} L09
\item \textbf{Termin zajęć laboratoryjnych:} Wtorek 9:45-11:15
\item \textbf{Termin oddania sprawozdania:} 5.05.2026
\item \textbf{Wersja sprawozdania:} pierwsza
\item \textbf{Opis zadania:}
Celem ćwiczenia była analiza implementacji algorytmu Sita Eratostenesa 
w środowisku równoległym i sekwencyjnym z wykorzystaniem biblioteki OpenMP. 
Zadanie polegało na analizie i modyfikacji podanych kodów, z róznymi 
implementacjami algorytmu, a następnie porównaniu ich wydajności.
\item \textbf{Adres e-mail:} emilia.bonk@student.put.poznan.pl, szymon.urban@student.put.poznan.pl
\end{itemize}

\section{Informacje techniczne.}

\begin{table}[h!]
\centering
\begin{tabular}{|l|l|}
\hline
\textbf{Parametr} & \textbf{Wartość} \\
\hline
Model procesora & 17th Gen Intel(R) Core(TM) i7-12700F \\
\hline
Liczba procesorów fizycznych & 1 \\
\hline
Liczba rdzeni & 12 (8 Performance, 4 Efficient)\\
\hline
Liczba procesorów logicznych & 20 \\
\hline
Pamięć cache L1 & 1,0 MB \\
\hline
Pamięć cache L2 & 12,0 MB \\
\hline
Pamięć cache L3 & 25,0 MB \\
\hline
L1 data cache dla EFfCore & 32 kB \\
\hline
L1 data cache dla PerfCore & 48 kB \\
\hline
System operacyjny & Windows 10 \\
\hline
Kompilator & Intel(R) oneAPI DPC++/C++ Compiler 2025.3.3 \\
\hline
Wersja kompilatora & 2025.3.3.20260319 \\
\hline
Program testujący & Intel VTune Profiler 2026.0.0\\
\hline
\end{tabular}
\caption{Informacje techniczne dotyczące środowiska testowego}
\end{table}


\newpage
\section{Prezentacja i analiza kodów.}

\subsection*{K1 - Algorytm sekwencyjny oparty na próbnych dzieleniach.}
\textbf{Oznaczenie skrótowe:} k1
\\ \\
Algorytm sekwencyjny oparty na metodzie dzielenia próbnego. Najpierw wyznacza liczby pierwsze w zakresie do $\sqrt{n}$, a następnie szeregowo sprawdza podzielność każdej liczby $i \in [m, n]$ przez te dzielniki. Koszt obliczeń jest zmienny – najniższy dla liczb parzystych, najwyższy dla liczb pierwszych.
\\

\begin{lstlisting}[style=mystyle, caption={Kod programu k1.cpp}, label={lst:k1}]
int calculate(int n, int m){
	bool* result = (bool*)malloc((n - m + 1) * sizeof(bool));
	memset(result, true, (n - m + 1) * sizeof(bool));
	int prime_counter = 0;

	bool* primeArray = (bool*)malloc((sqrt(n) + 1) * sizeof(bool));
	memset(primeArray, true, (sqrt(n) + 1) * sizeof(bool));
	
	if (m <= 0 && n >= 0) result[0 - m] = false;
	if (m <= 1 && n >= 1) result[1 - m] = false;

	for (int i = 2; i * i <= n; i++) {
		for (int j = 2; j * j <= i; j++) {
			if (primeArray[j] == true && i % j == 0) { 
				primeArray[i] = false; 
				break;
			}
		}
	}
	for (int i = m; i <= n; i++){
		for (int j = 2; j * j <= i; j++){
			if (primeArray[j] == true && i % j == 0) {
				result[i - m] = false;
				break; 
			} 
		}
		if(result[i-m] == true){
			prime_counter++;
		}
	}
	free(result);
	free(primeArray);

	return prime_counter;
}
\end{lstlisting}

\newpage

\subsection*{K2 - Liczby pierwsze wyznaczane równolegle przez dzielenie w zakresie <m,n>.}
\textbf{Oznaczenie skrótowe:} k2
\\ \\
Wariant równoległy wykorzystujący dzielenie próbne z dyrektywą \texttt{schedule(guided)}. Zastosowanie malejących paczek zadań pozwala na 
efektywne zrównoważenie obciążenia procesora, niwelując różnice w czasie sprawdzania małych i dużych liczb. Brak wyścigów przy zapisie do tablicy wyników.
\\
\begin{lstlisting}[style=mystyle, caption={Kod programu k2.cpp}, label={lst:k2}]
int calculate(int n, int m){
	int prime_counter = 0;

	//...liczenie sita (PrimeArray) jak w kodzie k1
	#pragma omp parallel
	{
	#pragma omp for schedule(guided) reduction(+:prime_counter)
	for (int i = m; i <= n; i++){
		for (int j = 2; j * j <= i; j++){
			if (primeArray[j] == true && i % j == 0) { 
				result[i - m] = false;
				break;
			}
		}	
		if(result[i-m] == true){
			prime_counter++;
		}
	}
	} 
	free(result);
	free(primeArray);
	
	return prime_counter;
}
\end{lstlisting}

\newpage

\subsection*{K3 - Sito sekwencyjne bez lokalności dostępu do danych.}
\textbf{Oznaczenie skrótowe:} k3
\\ \\
Sito Eratostenesa realizowane sekwencyjnie. Zamiast dzielenia, algorytm wykreśla wielokrotności liczb pierwszych $i \le \sqrt{n}$ w całym zakresie $[m, n]$. 
Charakteryzuje się niską złożonością obliczeniową $O(n \log \log n)$, ale ograniczoną lokalnością danych przy skokowym dostępie do tablicy wyników.
\\

\begin{lstlisting}[style=mystyle, caption={Kod programu k3.cpp}, label={lst:k3}]
int calculate(int n, int m){
	//...liczenie sita jak we wczesniejszych kodach 

	for (int i = 2; i*i <= n; i++){
		if (primeArray[i]) {
			int firstMultiple = (m / i);
			if (firstMultiple <= 1) {
				firstMultiple = i + i;
			} else if (m % i) { 
				firstMultiple = (firstMultiple * i) + i;
			} else {
				firstMultiple = (firstMultiple * i);
			}
			for (int j = firstMultiple; j <= n; j+=i) { 
				result[j-m] = false;
			}
		}
	}
	if (m <= 0 && n >= 0) result[0 - m] = false;
	if (m <= 1 && n >= 1) result[1 - m] = false;

	for (int i = m; i <= n; i++) {
    	if (result[i - m]) prime_counter++;
	}

	free(result);
	free(primeArray);
	
	return prime_counter;
}
\end{lstlisting}

\newpage
\subsection*{K3a - Sito sekwencyjne z lokalnością dostępu do danych.}
\textbf{Oznaczenie skrótowe:} k3a
\\ \\ 
Sekwencyjne sito segmentowe zoptymalizowane pod kątem lokalności danych. Zakres $[m, n]$ dzielony jest na bloki mieszczące się w pamięci cache L1/L2.
Przetwarzanie blokowe znacząco przyspiesza operacje zapisu do tablicy wyników poprzez redukcję pudeł cache.
\\

\begin{lstlisting}[style=mystyle, caption={Kod programu k3a.cpp}, label={lst:k3a}]
int calculate(int n, int m){
    //...alokacja pamieci i liczenie PrimeArray tak samo jak w kodzie k3

    int blockSize = 32768; 
    int numberOfBlocks = (n - m) / blockSize;
    if ((n - m) % blockSize != 0) { 
        numberOfBlocks++;
    }

    for (int i = 0; i < numberOfBlocks; i++) {
        int low = m + i * blockSize; 
        int high = low + blockSize - 1;
        if (high > n) {
            high = n;
        }

        for (int j = 2; j * j <= high; j++) {
            if (primeArray[j] == true) {
                int firstMultiple = (low / j);
                if (firstMultiple <= 1) {
                    firstMultiple = j + j;
                } else if (low % j) {
                    firstMultiple = (firstMultiple * j) + j;
                }  else {
                    firstMultiple = (firstMultiple * j);
                }
                for (int k = firstMultiple; k <= high; k += j) {
                    result[k - m] = false;
                }
            }
        }
    }

	//...zliczanie prime_count i zwalnianie pamieci jak w kodzie k3
}
\end{lstlisting}

\newpage
\subsection*{K4 - Sito równoległe funkcyjne bez lokalności dostępu do danych.}
\textbf{Oznaczenie skrótowe:} k4
\\ \\
Równoległe sito z podziałem funkcyjnym (\texttt{schedule(dynamic, 1)}). Wątki pobierają kolejne liczby pierwsze i wykreślają ich wielokrotności w 
całym zakresie wyników. Występuje zjawisko \texttt{false sharing} oraz bezpieczne wyścigi przy zapisie tej samej wartości logicznej pod ten sam indeks.
\\

\begin{lstlisting}[style=mystyle, caption={Kod programu k4.cpp}, label={lst:k4}]
int calculate(int n, int m){
	//...alokacja pamieci i lizcenie PrimeArray jak wczesniej 

	int limit = sqrt(n);
	#pragma omp parallel for schedule(dynamic, 1)
	for (int i = 2; i <= limit; i++){
		if (primeArray[i]) {
			int firstMultiple = (m / i);
			if (firstMultiple <= 1) {
				firstMultiple = i + i;
			} else if (m % i){ 
				firstMultiple = (firstMultiple * i) + i;
			} else {
				firstMultiple = (firstMultiple * i);
			}
			for (int j = firstMultiple; j <= n; j+=i) {
				result[j-m] = false;
			}
		}
	}

	//zliczanie prime_count i zwalnianie pamieci jak wczesniej
}
\end{lstlisting}

\newpage
\subsection*{K4a - Sito równoległe funkcyjne bez lokalności dostępu do danych z usunięciem redundancji zapisów.}
\textbf{Oznaczenie skrótowe:} k4a
\\ \\ 
Modyfikacja algorytmu K4 wprowadzająca warunkowy zapis do tablicy wyników (\texttt{if (result[j-m])}). Ma to na celu ograniczenie liczby operacji zapisu i związanych 
z nimi konfliktów w pamięci cache, co poprawia wydajność przy zachowaniu podziału funkcyjnego.
\\

\begin{lstlisting}[style=mystyle, caption={Kod programu k4a.cpp}, label={lst:k4a}]
int calculate(int n, int m){
	//...alokacja pamieci i zliczanie PrimeArray jak wczesniej

	int limit = sqrt(n);
	#pragma omp parallel for schedule(dynamic, 1)
	for (int i = 2; i <= limit; i++){
		if (primeArray[i]) {
			int firstMultiple = (m / i);
			if (firstMultiple <= 1) {
				firstMultiple = i + i;
			} else if (m % i){ 
				firstMultiple = (firstMultiple * i) + i;
			} else {
				firstMultiple = (firstMultiple * i);
			}
			for (int j = firstMultiple; j <= n; j+=i) {
				if (result[j-m]) result[j-m] = false;
			} 
		}
	}

	//zliczanie prime_counter i zwalnianie pamieci jak w k4
}
\end{lstlisting}


\newpage
\subsection*{K5 - Sito równoległe domenowe z lokalnością dostępu do danych}
\textbf{Oznaczenie skrótowe:} k5
\\ \\
Równoległe sito segmentowe z podziałem domenowym. Każdy wątek przetwarza niezależne bloki danych mieszczące się w cache, co eliminuje \texttt{false sharing} i wyścigi. 
Choć wydawałoby się, że \texttt{schedule(static)} minimalizuje narzut synchronizacji przy wyrównanym obciążeniu bloków,
to jednak znacznie lepszy performormance zaobserwowaliśmy przy \texttt{schedule(guided)}.
\\

\begin{lstlisting}[style=mystyle, caption={Kod programu k5.cpp}, label={lst:k5}]
int calculate(m, n){
	//...alokacja pamieci i liczenie PrimeArray jak wczesniej

	int blockSize = 32768; 
	int numberOfBlocks = (n - m) / blockSize + ((n - m) % blockSize != 0);

	#pragma omp parallel for schedule(guided)
	for (int i = 0; i < numberOfBlocks; i++) {
		int low = m + i * blockSize; 
		int high = low + blockSize - 1;
		if (high > n) high = n;

		for (int j = 2; j * j <= high; j++) {
			if (primeArray[j]) {
				int firstMultiple = (low / j) * j;
				if (firstMultiple < low) firstMultiple += j;
				if (firstMultiple == j) firstMultiple += j; 

				for (int k = firstMultiple; k <= high; k += j) {
					result[k - m] = false;
				}
			}
		}
	}

	//zliczanie prime_counter i zwalnianie pamieci jak wczesniej
}
\end{lstlisting}


\newpage
\section{Prezentacja wyników i omówienie przebiegu eksperymentu.}
\subsection*{Zasady pracy z oprogramowaniem VTune.}

Intel VTune Profiler to narzędzie do profilowania wydajności aplikacji na platformach Intel. 
W ramach przeprowadzonego eksperymentu wykorzystano tryb \textbf{Microarchitecture Exploration}, 
który umożliwia szczegółową analizę wykorzystania mikroarchitektury procesora poprzez zbieranie 
zdarzeń sprzętowych (hardware performance counters). Skorzystano również dla kodów 
równoległych z testu \textbf{Threading}, który pozwala na analizę efektywnego wykorzystania CPU oraz
podziału czasu pracy między sekcję równoległą a sekwencyjną.
W ramach analizy wariantów kodów k3, k3a, k4 i k4a został też przeprowadzony test \textbf{Memory Access}, który pozwolił
na analizę dostępów do pamięci.

\subsection*{Porównanie podstawowych parametrów wywołania dla kodów k1-k5.}
Porównania zostały wykonane dla kodów przedstawionych w poprzedniej sekcji, z tymi samymi schedulingami
i rozmiarami bloków na zakresie liczb od 1 do 100 milionów. Są one wynikiem testu \textbf{Microarchitecture Exploration}.
\begin{table}[h!]
\centering
\begingroup
% \resizebox{\textwidth}{!}{%
\begin{tabular}{|l|c|c|c|c|c|c|c|}
\hline
 & \textbf{k1} & \textbf{k2} & \textbf{k3} & \textbf{k3a} & \textbf{k4} & \textbf{k4a} & \textbf{k5} \\
\hline
Elapsed Time [s] & 23.750 & 2.394 & 0.660 & 0.130 & 0.381 & 0.296 & 0.060 \\
\hline
Prędkość przetwarzania [mln l. pierwszych/s] & 4.210 & 2.406 & 14.394 & 35.714 & 15.121 & 19.464 & 96.024 \\
\hline
Instructions retired [mld] & 405,744 & 405,599 & 0,515 & 0,777 & 2,179 & 1,501 & 1,702 \\
\hline
Clockticks [miliardów tików] & 105,112 & 166,746 & 2,914 & 0,566 & 19,808 & 14,777 & 1,995 \\
\hline
Effective PCU & 7.7\% & 68.9\% & 4.7\% & 7.1\% & 42.4\% & 51.5\% & 23.4\% \\
\hline
\textbf{Badania kodów równoległych} & \textbf{k1} & \textbf{k2} & \textbf{k3} & \textbf{k3a} & \textbf{k4} & \textbf{k4a} & \textbf{k5} \\
\hline
Przyspieszenie względem k3a & - & 0.05 & - & - & 0.34 & 0.43 & 2.17 \\
\hline
Efektywność [Acc/Cores] & - & 0.004 & - & - & 0.028 & 0.036 & 0.180 \\
\hline
\end{tabular}%
% }
\endgroup
\caption{Porównanie podstawowych parametrów wywołania dla kodów k1-k5.}
\end{table}
\begin{table}[h!]
\centering
\begingroup
% \resizebox{\textwidth}{!}{%
\begin{tabular}{|l|c|c|c|c|c|c|c|}
\hline
\textbf{Performance Cores} & \textbf{k1} & \textbf{k2} & \textbf{k3} & \textbf{k3a} & \textbf{k4} & \textbf{k4a} & \textbf{k5} \\
\hline
Retiring & 66.0\% & 83.5\% & 1.2\% & 0.0\% & 1.1\% & 4.5\% & 25.0\% \\
\hline
Front End Bound & 11.4\% & 13.8\% & 0.0\% & 3.1\% & 3.6\% & 20.0\% & 25.0\% \\
\hline
Back End Bound & 3.3\% & 1.7\% & 100.0\% & 0.0\% & 78.7\% & 58.0\% & 41.7\% \\
\hline
Memory Bound & 0.2\% & 0.2\% & 100.0\% & 0.0\% & 67.4\% & 42.6\% & 16.7\% \\
\hline
Core Bound & 3.1\% & 1.5\% & 0.0\% & 0.0\% & 11.4\% & 15.3\% & 25.0\% \\
\hline
\textbf{Efficient Cores} & \textbf{k1} & \textbf{k2} & \textbf{k3} & \textbf{k3a} & \textbf{k4} & \textbf{k4a} & \textbf{k5} \\
\hline
Retiring & 0.0\% & 42.3\% & 0.0\% & 0.0\% & 3.0\% & 0.7\% & 0.0\% \\
\hline
Front End Bound & 0.0\% & 5.0\% & 0.0\% & 0.0\% & 1.3\% & 3.8\% & 0.0\% \\
\hline
Back End Bound & 4.1\% & 11.5\% & 0.0\% & 0.0\% & 90.8\% & 34.8\% & 97.4\% \\
\hline
Memory Bound & 0.0\% & 0.2\% & 0.0\% & 0.0\% & 53.0\% & 34.8\% & 18.8\% \\
\hline
Core Bound & 4.1\% & 11.3\% & 0.0\% & 0.0\% & 37.8\% & 0.0\% & 78.6\% \\
\hline
\end{tabular} 
% }
\endgroup
\caption{Wyniki analizy przyczyn ograniczeń wydajności dla kodów k1-k5 z podziałem na Performance i Efficient Cores.}
\end{table}
\newpage
\subsection*{Porównanie działań kodów dla różnych zakresów liczb.}
W ramach tego eksperymentu zmierzyliśmy Ellapsed Time dla kodów k1-k5 dla trzech
różnych zakresów liczb: \\<2; max>, <max/2; max> oraz <2; max/2>, gdzie \texttt{max} to 10\textsuperscript{8}.
\begin{table}[h!]
\centering
\begingroup
% \resizebox{\textwidth}{!}{%
\begin{tabular}{|l|c|c|c|c|c|c|c|}
\hline
\textbf{Zakres liczb} & \textbf{k1} & \textbf{k2} & \textbf{k3} & \textbf{k3a} & \textbf{k4} & \textbf{k4a} & \textbf{k5} \\
\hline
<2; max> & 23.750 s & 2.394 s & 0.660 s & 0.130 s & 0.381 s & 0.296 s & 0.060 s \\
\hline
<max/2; max> & 16.671 s & 3.852 s & 0.303 s & 0.069 s & 0.279 s & 0.122 s & 0.038 s \\
\hline
<2; max/2> & 7.116 s & 2.450 s & 0.282 s & 0.065 s & 0.225 s & 0.117 s & 0.050 s \\
\hline


\end{tabular}%
% }
\endgroup
\caption{Porównanie prędkości działania dla różnych zakresów liczb w kodach k1-k5.}
\end{table}
\\
Porównanie zakresów  ujawnia różne zachowania algorytmów w zależności od położenia zakresu.
Dla \textbf{k1 i k2} (dzielenie próbne) czas silnie zależy od gęstości liczb pierwszych 
i wartości sprawdzanych liczb. Zakres $\langle \text{max}/2; \text{max} \rangle$ trwa 
dłużej niż $\langle 2; \text{max}/2 \rangle$, ponieważ 
duże liczby wymagają sprawdzenia więcej dzielników (do $\sqrt{i}$).
Dla \textbf{k3, k3a, k4, k4a, k5} (sito) czas nie zależy od wartości sprawdzanych 
liczb, lecz przede wszystkim od \textbf{rozmiaru zakresu}. Złożoność segmentowego sita 
Eratostenesa dla zakresu $\langle m, n \rangle$ wynosi $O(L \cdot \log \log n)$, 
gdzie $L = n - m$ to rozmiar zakresu, a $n$ to górna granica. Ponieważ $\log \log n$ 
zmienia się bardzo wolno i jest niemal identyczne dla obu półzakresów, czas przetwarzania jest w przybliżeniu proporcjonalny do $L$ — 
stąd oba półzakresy mają zbliżone czasy, mniejsze niż pełny zakres.

\subsection*{Badanie wpływu różnych szeregowań dla czasu przetwarzania kodów k2, k4, k4a i k5.}	
Test został przeprowadzony na trybie \textbf{Microarchitecture Exploration}, a wyniki Ellapsed Time
zostały umieszczone w Tablicy~5. Porównano trzy różne szeregowania: \texttt{schedule(static)}, \texttt{schedule(dynamic)} i \texttt{schedule(guided)}.
\\
\begin{table}[h!]
\centering
\begingroup
% \resizebox{\textwidth}{!}{%
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Schedule} & \textbf{k2} & \textbf{k4} & \textbf{k4a} & \textbf{k5} \\
\hline
static & 6.339 s & 0.551 s & 0.541 s & 0.151 s \\
\hline
dynamic & 2.475 s & 0.296 s & 0.381 s & 0.080 s \\
\hline
guided &  2.394 s & 0.558 s & 0.727 s & 0.060 s \\
\hline

\end{tabular}%
% }
\endgroup
\caption{Wpływy szeregowań na czas przetwarzania kodów równoległych.}
\end{table}
\\
Dla \textbf{k2} (dzielenie próbne) \texttt{schedule(guided)} (2,394~s) i 
\texttt{schedule(dynamic)} (2,475~s) są zbliżone i znacznie lepsze od 
\texttt{schedule(static)} (6,339~s). Wynika to z nierównomiernego kosztu sprawdzania 
poszczególnych liczb — statyczny podział przydziela jednym wątkom wyłącznie duże liczby 
(droższe), innym — małe (tańsze), powodując silne niezrównoważenie. \\ \\
Dla \textbf{k4 i k4a} (sito funkcyjne) \texttt{schedule(dynamic)} okazuje się lepszy 
od \texttt{schedule(guided)}, gdyż zadania (kolejne liczby pierwsze do wykreślenia) 
mają bardzo zróżnicowany koszt — mała liczba pierwsza (np. 2) generuje wiele 
wielokrotności, duża (np. 9999991) — nieliczne. Dynamiczny przydział po jednym zadaniu 
na raz najlepiej to wyrównuje. \\ \\
Dla \textbf{k5} (sito domenowe) \texttt{schedule(guided)} (0,060~s) jest najlepsze, 
bo bloki danych mają zbliżony koszt przetwarzania, a malejące paczki zadań redukują 
narzut synchronizacji przy końcu pętli, kiedy wątki kończą pracę w różnym czasie.

\newpage
\subsection*{Porównanie wyników przetwarzania kodów k3 i k3a.}
W celu głębszej analizy przyczyn różnicy w czasie przetwarzania między k3 (0,660~s)
a k3a (0,130~s) przeprowadzono dodatkowy eksperyment z wykorzystaniem testu
\textbf{Memory Access}.
Wyniki przedstawiono na Rysunkach~1 i~2 oraz w Tablicy~6.
\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{img/k3.png}
    \caption{Wynik testu Memory Access BandwitdhHistogram dla kodu k3.}
    \label{fig:k3treading109}
\end{figure} 
\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{img/k3a.png}
    \caption{Wynik testu Memory Access BandwitdhHistogram dla kodu k3a.}
    \label{fig:k3atreading109}
\end{figure}

Możemy zobaczyć, że średnia przepustowość k3 jest praktycznie 3 razy większa
niż k3a. Są to około 32 GB/s, dochodząc nawet do 38GB/s. W kodzie k3 średnia wartość
to już 11GB/s, ilość danych przechodzących przez magistralę jest znacznie mniejsza.
Wynika to z naszej zmiany w kodzie k3a - zmieniamy pojedynczą iterację na przetwarzającą blok,
a nie konkretną liczbę, dzięki czemu aktywny rozmiar tablicy mieści się w L1. W k3 zaś
wykreślamy dla każdej iteracji liczby na całym zakresie, przez co każdy zapis trafia w odległe od
siebie miejsca w pamięci.
\\
\begin{table}[h!]
\centering
\begingroup
% \resizebox{\textwidth}{!}{%
\begin{tabular}{|l|c|c|}
\hline
 & \textbf{k3} & \textbf{k3a} \\
\hline

P-Core L1 Bound & 0.0\% & 0.0\% \\
\hline
P-Core L2 Bound & 1.1\% & 0.0\% \\
\hline
P-Core L3 Bound & 1.5\% & 0.0\% \\
\hline
P-Core Store Bound & 82.6\% & 0.0\% \\
\hline
E-Core L1 Bound & 0.0\% & 1.6\% \\
\hline
E-Core L2 Bound & 0.0\% & 0.0\% \\
\hline
E-Core	 L3 Bound & 0.0\% & 0.0\% \\
\hline

\end{tabular}%
% }
\endgroup
\caption{Porównanie czasów oczekiwania Memory Bound dla kodów k3 i k3a}
\end{table}
\\

Aby lepiej zrozumieć problem przyjrzeliśmy się również przyczynom ograniczeń 
wydajności dla obu kodów. Możemy zobaczyć, że k3 jest w 82,6\% ograniczony 
przez oczekiwanie na przepełniony store buffer. Jest on więc dla naszego kodu 
wąskim gardłem — liczba żądań zapisu przerasta pojemność bufora zapisu procesora. 
Możemy też zauważyć że w kodzie k3 ograniczenia pamięci występują na L2 i L3; wynika to z tego że pętla wykreślająca wielokrotności 
przechodzi przez całą tablicę \texttt{result} o rozmiarze 100~mln elementów. 
Tablica ta nie mieści się w L1 ani L2 pojedynczego rdzenia, więc przy każdym 
dostępie procesor musi załadować odpowiednią linię cache z L2 lub L3.
\\ \\

W kodzie k3a obraz jest zupełnie odmienny. Przetwarzanie blokowe sprawia że aktywny 
obszar tablicy wynosi zawsze 32~kB i powinien mieścić się w L1 zarówno E-core jak i L1 P-core. Dla każdego bloku 
wszystkie dzielniki $i \leq \sqrt{\text{high}}$ są przetwarzane zanim algorytm 
przejdzie dalej - linie cache pozostają w L1 przez cały czas przetwarzania bloku 
i są wielokrotnie wykorzystywane. Stąd zerowe L2 Bound, L3 Bound i Store Bound - 
jedyne ograniczenie występuje na E-Core L1 Bound, następuje lekkie przepełnienie na pierwszej pamięci cache,
czego jednak można było się spodziewać, jest ona mniejsza niż L1 P-core, mając dokładnie 32kB zamiast
48kB jak w wypadku P-Core.

\newpage

\subsection*{Analiza wyników przetwarzania kodów k4 i k4a.}
W tym eksperymencie poddaliśmy teście \textbf{Memory Access} kody k4 i k4a,
aby zobaczyć jak dodanie warunku \texttt{if result[j-m]} wpływa na ograniczenia związane z pamięcią,
i tym samym czas wykonania kodu. Wyniki przedstawiono w Tablicy~7.

\begin{table}[h!]
\centering
\begingroup
\begin{tabular}{|l|c|c|}
\hline
 & \textbf{k4} & \textbf{k4a} \\
\hline
P-Core Memory Bound & 81.4\% & 28.3\% \\
\hline
P-Core L1 Bound & 2.9\% & 7.3\% \\
\hline
P-Core L2 Bound & 1.3\% & 0.8\% \\
\hline
P-Core L3 Bound & 3.6\% & 4.6\% \\
\hline
P-Core Store Bound & 72.3\% & 0.3\% \\
\hline
E-Core Memory Bound & 77.8\% & 58.1\% \\
\hline
E-Core L1 Bound & 0.6\% & 1.5\% \\
\hline
E-Core L2 Bound & 0.0\% & 0.2\% \\
\hline
E-Core	 L3 Bound & 0.0\% & 0.3\% \\
\hline
Loads & 94,902,847 & 389,711,691 \\
\hline
Stores & 241,807,254 & 155,704,671 \\
\hline
\end{tabular}
\endgroup
\caption{Porównanie czasów oczekiwania Memory Bound dla kodów k4 i k4a}
\end{table}

Kod \textbf{k4} na obu rodzajach rdzeni uzyskał wyższe Memory Bound niż \textbf{k4a}.
Na rdzeniu Performance możemy ponownie jak w wypadku kodu \textbf{k3} zobaczyć 
wysoki Store Bound (72.3\%), który wiąże się z dużą liczbą operacji zapisu do tablicy 
\texttt{result} oraz ponownie przemieszczaniu się po całej tablicy.
Stosunek Loads do Stores dla k4 wynosi około 1:2, odbywa się więcej zapisów niż odczytów,
wielokrotnie bowiem nadpisujemy wartość \texttt{false} pod ten sam indeks.
Dodanie warunku \texttt{if (result[j-m])} w kodzie k4a redukuje liczbę zapisów, 
zmieniając proporcje Loads/Stores do około 2.5:1, znacząco redukuje to Store Bound i poprawia 
wyniki na Memory Bound. Nie usuwa jednak problemu w całości, wciąż występują ograniczenia na pamięciach cache L1, L2, i L3.
Dodanie warunku tworzy nowy koszt w postaci dodatkowych odczytów, sprawiając, że choć udało nam się zredukować Store Bound,
kod wcale nie jest wolny od ograniczeń związanych z pamięcią i wykazuje się jedynie niewielką poprawą czasu wykonania.

\subsection*{Wyniki eksperymentu wyznaczania wielkości bloku dla kodów k3a i k5.}

Aby wyznaczyć optymalną wielkość bloku dla kodów k3a i k5 napisaliśmy eksperyment,
który wywoływał te kody dla standardowego zakres <2; max> z różnymi rozmiarami bloków
od 16B do ilości liczb w zakresie z krokiem x2. Poniżej znajduje się część tabeli wynikowej,
w której na środku znajduje się nasze poszukiwane optimum.
\\
\begin{table}[h!]
\centering
\begingroup
% \resizebox{\textwidth}{!}{%
\begin{tabular}{|l|c|c|}
\hline
\textbf{Blocksize} & \textbf{k3a} & \textbf{k5} \\
\hline

4096 & 0.382 s & 0.061 s \\
\hline
8192 & 0.229 s & 0.040 s \\
\hline
16384 & 0.145 s & 0.036 s \\
\hline
32768 & 0.107 s & 0.030 s \\
\hline
65536 & 0.120 s & 0.031 s \\
\hline
131072 & 0.121 s & 0.039 s \\
\hline
262144 & 0.126 s & 0.035 s \\
\hline
524288 & 0.128 s & 0.040 s \\
\hline

\end{tabular}%
% }
\endgroup
\caption{Wpływ rozmiaru bloku na czas przetwarzania kodów k3a i k5.}
\end{table}
\\
Optymalny rozmiar bloku wynosi \textbf{32768 elementów} (32~kB) dla obu kodów 
k3a i k5. Jest to wynik, którego możemy się spodziewać po sprawdzeniu parametrów
procesora: choć posiada on ogółem 1 MB na pamięci L1, podzielona jest ona między 12 rdzeni,
z czego 8 to rdzenie Performance z 48 kB cache L1, a pozostałe 4 to rdzenie Efficient z 32 kB
cache L1. Z tych względów rozmiar bloku 32 kB będzie optymalny, będzie mieścił się w cache L1 
obu typów rdzeni, jednocześnie będąc na tyle dużym aby zredukować niepotrzebny narzut związany z pobieraniem
mniejszych ale większej liczby bloków z pamięci głównej do cache. Większy blok nie mieściłby się już zaś w L1, zmuszając
do przenoszenia danych do wolniejszej pamięci L2, co ponownie zaczyna skutkować spadkiem wydajności.

\newpage
\subsection*{Dodatkowy eksperyment: zwiększenia zakresu liczb do 1 miliarda.}
W czasie przetwarzania kodów równoległych w standardowym zakresie <2;100 mln> zauważyliśmy,
że czasy przetwarzania są bardzo krótkie, co utrudnia dokładną analizę wydajności.
Dlatego zdecydowaliśmy się na dodatkowy eksperyment, w którym zwiększyliśmy zakres liczb do 1 miliarda.
Poniżej przedstawiamy porównania wyników testu \textbf{Threading} dla kodu równoległego k4.

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{img/k4108.png}
    \caption{Wynik testu threading k4 dla zakresu <2; 100 mln>.}
    \label{fig:k4treading108}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=1\textwidth]{img/k4109.png}
    \caption{Wynik testu threading k4 dla zakresu <2; 1 mld>.}
    \label{fig:k4treading109}
\end{figure}

Pierwsze co rzuca się przy analizie wyżej przedstawionych wykresów to 
znaczny wzrost efektywności CPU z 26.3\% na 63.7\% po zwiększeniu zakresu. 
Czas spędzony na pracy 0 lub 1 wątku pozostaje w obu wypadkach taki sam: w sumie około 0.6 s.
Cały dodatkowy czas wykonania to czas spędzony w sekcji równoległej, który wzrósł z 0.267 s na 
4.871 s. Z wykresu widać że najwięcej czasu kod wykonuje jednocześnie 14 wątków, a nie 1 jak 
w przypadku rysunku 1. Podobne wyniki zaobserwowaliśmy dla reszty równoległych kodów. Widzimy więc,
że niska wydajność kodów nie wynika z ich złej implementacji, a zbyt małego zakresu liczb w teście, 
który nie pozwalał na efektywne wykorzystanie potencjału równoległego procesora.

\newpage
\section{Wnioski.}


\end{document}
